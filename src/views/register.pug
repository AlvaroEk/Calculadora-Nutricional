extends navbar

block content2
  h1 Registrarse
  form(action='/user/register', method='POST')
    label(for='nombre') Nombre:
    input.input-login(type='text', name='nombre', required)
    label(for='correo') Correo:
    input.input-login(type='email', name='correo', required)
    label(for='password') Contraseña:
    input.input-login(type='password', name='password', required)
    button(type='submit') Registrarse
