extends navbar

block content2
  h1 Iniciar sesión
  form(action='/user/login', method='POST')
    label(for='correo') Correo:
    input.input-login(type='email', name='correo', required)
    label(for='password') Contraseña:
    input.input-login(type='password', name='password', required)
    button(type='submit') Iniciar sesión
    ul 
      a(href='/register') Registro
    block content
